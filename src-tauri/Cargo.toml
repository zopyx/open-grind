[package]
name = "open-grind"
version = "0.1.0-beta.5-dev"
description = "FLOSS Grindr client"
authors = ["Open Grind Governance (https://opengrind.org/governance)"]
homepage = "https://opengrind.org"
repository = "https://git.opengrind.org/open-grind/open-grind"
license = "MIT"
readme = "../README.md"
publish = false
edition = "2021"

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "open_grind_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[features]
keychain = ["dep:apple-native-keyring-store"]
private-api = []
devtools = ["dep:tauri-plugin-devtools"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dev-dependencies]
tauri = { version = "2", features = ["test"] }
tokio = { version = "1", features = ["net", "io-util", "rt-multi-thread"] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
grindr = "0.19"
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "webp", "gif", "bmp", "tiff"] }
moxcms = "0.8"
tokio = { version = "1", features = ["sync", "time", "rt", "macros"] }
wreq = { version = "0.15", default-features = false }
ed25519-dalek = { version = "2", default-features = false, features = ["alloc"] }
blake2 = { version = "0.10", default-features = false }
semver = "1"
tauri-plugin-fs = "2"
tauri-plugin-geolocation = "2"
tauri-plugin-os = "2"
tauri-plugin-clipboard-manager = "2"
rmp-serde = "1.3.1"
serde_bytes = "0.11"
base64 = "0.22"
getrandom = "0.3"
keyring-core = "1.0.0"
tauri-plugin-devtools = { version = "2.0.0", optional = true }
tauri-plugin-dialog = "2"
tracing = { version = "0.1", features = ["release_max_level_off"] }
# Local SQLite for saved phrases; bundled so every platform links the same
# SQLite instead of whatever the system happens to ship.
rusqlite = { version = "0.40", features = ["bundled"] }

[target.'cfg(target_os = "macos")'.dependencies]
apple-native-keyring-store = { version = "1.0.1", features = ["keychain", "protected"], optional = true }
# Private WKWebView selector behind `-apple-visual-effect`, see src/appearance.rs
objc2 = "0.6"
# Scroll-wheel gesture phases for the debug probe, see src/scroll_phase.rs
block2 = "0.6"
objc2-app-kit = { version = "0.3", default-features = false, features = ["std", "NSEvent", "NSHapticFeedback", "block2", "objc2-core-foundation"] }
rfd = "0.16"

[target.'cfg(target_os = "ios")'.dependencies]
apple-native-keyring-store = { version = "1.0.1", features = ["keychain", "protected"] }

[target.'cfg(target_os = "windows")'.dependencies]
windows-native-keyring-store = "1.1.0"
webview2-com = "0.38"
windows = { version = "0.61", features = ["Win32_System_Com"] }
windows-core = "0.61"

[target.'cfg(target_os = "linux")'.dependencies]
dbus-secret-service-keyring-store = { version = "1.0.0", features = ["crypto-rust"] }

[target.'cfg(target_os = "android")'.dependencies]
android-native-keyring-store = "1.0.0"
# Reads the EmbeddedSetup oauth_token cookie via android.webkit.CookieManager
jni = "0.21"
tauri-plugin-android-fs = "=28.2.2"

[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-single-instance = "2"

[profile.release]
strip = "symbols"

# See https://github.com/tauri-apps/tauri/pull/15777
[patch.crates-io]
wry = { path = ".patched/wry" }
tauri-codegen = { path = ".patched/tauri-codegen" }
http2 = { path = ".patched/http2" }
tauri-plugin-geolocation = { path = ".patched/tauri-plugin-geolocation" }
